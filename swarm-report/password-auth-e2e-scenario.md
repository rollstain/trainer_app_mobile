# Критпути авторизации

Прогон 29.08.2026, 21:50. Платформы: бэкенд и клиентская логика (Android UI глазами не проверялся —
Mobile MCP в сессии не подключён).

**237 тестов, падений нет.** Из них про авторизацию 70: бэкенд 44, клиент 26.

Запуск:

```
gradlew -p trainer_backend cleanTest test
gradlew -p trainer_app :androidApp:testDebugUnitTest --rerun-tasks
```

## Регистрация

- [x] Регистрация по почте и паролю открывает сессию · `PasswordAuthServiceTest`, `AuthCriticalPathTest`
- [x] Почта хранится в нижнем регистре, имя без лишних пробелов · `PasswordAuthServiceTest`
- [x] Логин необязателен; пустой не отправляется · `AuthCriticalPathTest`
- [x] Занятая почта — 409 с указанием поля, сессия не заводится · оба слоя + `AuthScreenFlowTest`
- [x] Занятый логин — ошибка у поля логина с предложением замены, введённое не стирается · `AuthScreenFlowTest`
- [x] Пароль короче восьми не принимается; подпись считает вслух до отправки · `PasswordAuthServiceTest`, `AuthScreenFlowTest`
- [x] После регистрации имя запомнено — предложение Telegram знает, к кому обращается · `AuthScreenFlowTest`

## Вход

- [x] Вход по почте · `PasswordAuthServiceTest`, `AuthCriticalPathTest`
- [x] Вход по логину, когда идентификатор не похож на почту · оба слоя
- [x] Неверная пара — общая ошибка без подсказки, что именно неверно · `PasswordAuthServiceTest`, `AuthCriticalPathTest`
- [x] Неверная пара не стирает набранный пароль · `AuthScreenFlowTest`
- [x] Отказ сети предлагает повтор, а не выглядит как неверный пароль · `AuthScreenFlowTest`
- [x] Пятая неудача запирает вход; ответ несёт `Retry-After` и секунды · `PasswordAuthServiceTest`, `AuthCriticalPathTest`
- [x] Каждая следующая блокировка вдвое дольше, потолок 30 минут · `PasswordAuthServiceTest`
- [x] Счётчик на экране тикает и по истечении срока открывает вход сам · `AuthScreenFlowTest`
- [x] Успешный вход обнуляет счётчик неудач · `PasswordAuthServiceTest`

## Восстановление письмом

- [x] Просьба о письме уходит на `/auth/password/forgot`, сессия не заводится · `AuthCriticalPathTest`
- [x] Неизвестная почта не получает письма и не выдаёт себя ответом · `PasswordResetServiceTest`
- [x] В базе лежит только хеш ссылки, не сама ссылка · `PasswordResetServiceTest`
- [x] Повтор чаще двух минут не шлёт второе письмо и не гасит живую ссылку · `PasswordResetServiceTest`
- [x] Повтор после окна заменяет прежнюю ссылку · `PasswordResetServiceTest`
- [x] Экран закрывает повтор на две минуты и открывает обратно · `AuthScreenFlowTest`
- [x] Отказ почтовой службы показывается как отказ, а не как молчание · оба слоя
- [x] Ссылка из письма задаёт пароль, отзывает прежние сессии и открывает новую · `PasswordResetServiceTest`, `AuthCriticalPathTest`
- [x] Использованная и просроченная ссылки различимы и ведут на разные экраны · оба слоя
- [x] Письмо доходит до живого ящика во «Входящие» · **проверено руками на проде 29.08.2026** (Яндекс и Gmail)

## Восстановление через Telegram

- [x] Сброс идёт на свою ручку, отдельную от почтовой · `AuthCriticalPathTest`
- [x] Неподтверждённый claim не открывает сессию и просит повторить · `AuthScreenFlowTest`
- [x] Telegram, не привязанный ни к одному аккаунту, получает отказ · `PasswordResetServiceTest`
- [x] Сброс заводит пароль тому, у кого его не было · `PasswordResetServiceTest`
- [x] Подтверждение живёт 15 минут, ссылка входа разменивается один раз · `TelegramLoginServiceTest`
- [ ] Полный путь с живым ботом — **не проверялся**: нужен телефон с Telegram

## Пароль и почта в профиле

- [x] Смена пароля требует текущий; неверный — отказ, а не разлогин · `PasswordAuthServiceTest`, оба слоя клиента
- [x] Смена пароля выводит остальные устройства, текущее остаётся · `PasswordAuthServiceTest`
- [x] Первый пароль требует почту, если её нет, и принимает переданную · `PasswordAuthServiceTest`
- [x] Форма отправляет только заполненное · `AuthCriticalPathTest`
- [x] Пароль считается способом входа — последний отвязать нельзя · `ExternalAuthServiceTest`
- [x] `/me` отдаёт логин, признак пароля и дату его смены · **проверено на проде**

## Сессии

- [x] Обновление токена выдаёт новую пару и помнит предыдущую · `SessionRotationTest`
- [x] Повтор старого токена в окне даёт рабочую пару, после окна отзывает сессию · `SessionRotationTest`
- [x] Срок скользящий: простой 90 дней, потолок год · `SessionRotationTest`
- [x] Выход роняет сессию · `AuthCriticalPathTest`

## Что тестами не закрыто

- **Вёрстка экранов**: отступы, поведение с клавиатурой, тёмная тема, крупный шрифт. Проверяется
  глазами или рендер-тестами по образцу `ScreenRenderTest`.
- **Живое устройство**: переход по ссылке из письма в приложение, автозаполнение менеджером паролей,
  вход через бота целиком.
- **Подтверждение почты при регистрации** — функции пока нет.
