# E2E Scenario: Подтверждение почты при регистрации
Платформы: Backend, Android

## Шаги
- [x] 1. Юниты бэкенда: `gradlew test` в trainer_backend — все зелёные ✅ (проверено 30.08.2026)
- [x] 2. Detekt бэкенда чист ✅
- [x] 3. Скрипт `scripts/check_migrations.py` не находит пересечений имён для V35 ✅
- [x] 4. Юниты клиента: `gradlew :androidApp:testDebugUnitTest` целиком — зелёные ✅
- [x] 5. Detekt клиента чист ✅
- [x] 6. Клиент собирается: `gradlew :androidApp:assembleDebug` ✅
- [x] 7. Регистрация с новой почтой → письмо отправляется ✅ (проверено на проде 30.08.2026: SMTP принял
  без ошибок в логе, resend вернул 204; письма ушли на hoshpah+trainer-e2e@gmail.com — «Входящие» глазами
  не смотрел, при желании сверить в ящике)
- [x] 8. Клик по ссылке → адрес подтверждается ✅ (30.08.2026 владелец прошёл на живом телефоне:
  письмо → приложение → тост «Почта подтверждена»; в базе `email_confirmed_at` проставлен,
  токен погашен)
- [x] 9. `/me` отдаёт `emailConfirmed: true` после подтверждения ✅ (плашка в профиле строится из этого
  поля; сам экран на устройстве не прогнан)
- [x] 10. Повторный клик той же ссылкой → 409 «Ссылка уже использована» ✅
- [x] 11. Регистрация вторым аккаунтом на занятый неподтверждённый адрес → 200, адрес переходит новому,
  у прежнего `email: null` при живой сессии ✅; на подтверждённый адрес → 409 с `fieldErrors.email` ✅
- [x] 12. Повтор письма: раньше двух минут → 429 с `Retry-After` и `retryAfterSeconds`, после окна → 204 ✅

Проверено на проде (V35) через curl и вставку тестового токена в базу; тестовые аккаунты и их строки
удалены, база возвращена к исходным двум пользователям. Осталось руками: клик по письму на телефоне
(диплинк → тост → плашка исчезает). Три тестовых письма в ящике мертвы — их ссылки дадут
«Ссылка не найдена», можно удалить.
