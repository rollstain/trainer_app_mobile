# Роадмап trainer_app

Состояние на 30.08.2026. Файл канонический — правится здесь, в репозитории `trainer_app`.
Что и почему уже сделано — в `changelog.md`, он же источник для «готово».

## Где мы сейчас

Приложение работает целиком на Android: расписание со слотами и группами, дневники тренировок,
программы, чек-ины с фото, видеоразбор техники, чат с вложениями, справочник упражнений.
Вход — код от тренера, ссылка-приглашение, Telegram, почта с паролем и восстановлением.
Прод на `api.lyashukfit.ru`, миграции до V34, пуши и хранилище заведены.

**Чего нет — людей.** В базе один пользователь, владелец, и ни одного тренера. Всё, что ниже, —
про то, чтобы их можно было впустить и не пожалеть.

| | |
|---|---|
| Платформы | Android; iOS не начат |
| Вход | код · ссылка · Telegram · почта и пароль |
| Соцвход | нет, ждёт ключей |
| Прод | V34, чистая база |

## Этап 1. Впустить первых тренеров

Цель: дать ссылку незнакомому человеку и не получить юридический или денежный хвост.
Пока эти три пункта открыты, самозапись работает, но приглашать некого.

### 1.1 Правовые тексты вычитывает юрист · держит вас

Политика и оферта опубликованы на `/legal/privacy` и `/legal/terms`, ссылки в согласии
на приветствии и регистрации ведут туда, реквизиты оператора подставляются из окружения.

Тексты — черновик по фактам проекта. Пробел, который кодом не закрыть: **медицинские пометки
тренера — специальная категория персональных данных** по 152-ФЗ, и обычного согласия действием
для них может не хватать — закон требует письменной формы или приравненной к ней.

_Готово, когда:_ юрист вычитал оба текста и сказал, в какой форме собирать согласие на медданные.

### 1.2 Квоты и тариф · решение, потом код

Самозапись открыта всем, а хранилище под видео и фото и объём пушей не ограничены ничем.
Один человек с сотней подопечных или десяток ботов — и счёт за Object Storage растёт молча,
узнаете вы об этом из счёта.

Решить надо продуктово: сколько мегабайт и подопечных даём бесплатно, что делаем при превышении.
После ответа это обычная работа на пару дней — счётчик на аккаунт и отказ на границе.

_Готово, когда:_ у аккаунта есть потолок, и при его достижении загрузка отвечает отказом, а не растёт.

### 1.3 Размер файла проверяется на слово · держит вас

Бэкенд верит числу `sizeBytes` из запроса, когда выдаёт ссылку на загрузку, и реально залитый
объём не сверяет: лимит в 25 МБ обходится подменой числа. Лечится политикой бакета, кодом — нет.

_Готово, когда:_ бакет сам режет загрузку сверх лимита.

## Этап 2. Не терять людей на входе

Цель: человек входит привычной кнопкой и не может увести чужой адрес.

### 2.1 Соцвход · держит вас

VK ID упёрся в подтверждение ИП: без него не отдают «Защищённый ключ». Кнопки Яндекса, VK,
Google и Apple без `client_id` включить нечем — код провайдеров написан и ждёт ключей.

_Готово, когда:_ хотя бы один провайдер отдал ключи и кнопка работает на живом телефоне.

### 2.2 Подтверждение почты при регистрации · сделано 30.08.2026

Адрес при регистрации и смене в профиле встаёт неподтверждённым, уходит письмо со ссылкой
на три дня; повтор — из «Способов входа», не чаще раза в две минуты. Занятый подтверждённым
адрес — отказ как раньше; занятый неподтверждённым отдаётся новому регистранту, а клик
по письму авторитетен и возвращает адрес настоящему владельцу ящика. Вход ничем не блокируется.
Прод на V35, путь «регистрация → письмо → клик → подтверждено» пройден на живом телефоне.

_Готово, когда:_ неподтверждённый адрес не блокирует регистрацию настоящему владельцу. **Сделано.**
Сознательно не сделано: экрана «мы отправили письмо» после регистрации нет — статус виден
только в профиле; клик по ссылке сброса пароля адрес не подтверждает.

## Этап 3. iOS

Цель: половина тренеров ходит с айфонами, сейчас им нечего поставить.

Общий код компилируется под три iOS-таргета, и это создаёт ложное впечатление готовности:
**папки `iosApp` нет вовсе** — ни Xcode-проекта, ни `AppDelegate`, ни обмена APNs-токена на FCM.
Это не «доделать», а «начать».

Нужны: Apple Developer Program (платный, держит вас), ключ `.p8`, `GoogleService-Info.plist`.
Порядок и грабля донора — `getMessagingToken` обязан вернуть `nil`, пока нет `apnsToken` —
в `push-and-media-setup.md`.

_Готово, когда:_ сборка ставится на айфон и пуш доходит.

## Этап 4. Выдержать рост

Цель: чтобы первые сто подопечных не уронили то, что работало на пяти. Раньше времени эти пункты
не окупятся — но и откладывать до жалоб не стоит.

### 4.1 Пагинация не везде · сделано 30.08.2026

Курсор в `X-Next-Cursor` теперь во всех списках, которые растут вместе с числом подопечных:
диалоги, очередь чек-инов, очередь видеоразборов и своя история разборов, программы тренера,
заявки на перенос. Справочник упражнений и история сообщений в диалоге были пагинированы раньше —
роадмап называл их по инерции.

Два списка закрыты не курсором, потому что это не списки, а справки под видимый экран:
пропуски занятий спрашиваются по `clientUserIds` показанной страницы людей, а заявки на перенос
на экране расписания — по диапазону дат недели.

_Готово, когда:_ ни один список не грузится целиком. **Сделано;** осталось прогнать нативные
запросы по живой схеме — статически имена таблиц и колонок сверены с миграциями, синтаксис
Postgres проверит только первый запрос после деплоя.

### 4.2 Вложение целиком в памяти · код

Событие с файлом несёт `ByteArray`: на 25 МБ видео это заметный пик, на слабом телефоне — риск
падения. Нужна потоковая загрузка, при этом меняется контракт репозитория вложений.

_Готово, когда:_ 25 МБ уходят на слабом телефоне, не поднимая пик памяти на весь файл.

### 4.3 Часовой пояс меняется один раз · код

Ставится при создании рабочего места, с телефона, и больше никак: в `CoachPolicyResponse` его нет.
Переехал тренер в другой пояс — расписание разъезжается у всех его подопечных.
Раздел «Как со мной работать» под это заложен.

_Готово, когда:_ тренер меняет пояс в профиле, и будущие занятия пересчитываются.

## Рынок: четырнадцать аналогов

Разобраны 30.08.2026: восемь западных платформ полного цикла, российский рынок и приложения
одной функции. Из каждого — одно: что взять себе. Подробный разбор с ценами и источниками —
в артефакте «Роадмап trainer_app».

| Кто | Суть | Что взять себе |
|---|---|---|
| ABC Trainerize | самый широкий набор: AI-конструктор, автопрогрессия %1RM, привычки, Stripe; питание — платный аддон, калории — интеграция MyFitnessPal | автопрогрессия в программах; питание как аддон, а не ядро |
| TrueCoach ($30–166/мес) | асинхронный видеофидбек на подходы, процент комплаенса, дашборд «кто соскочил» | дашборд комплаенса; ближайший по духу продукт |
| Everfit | автоматизации Autoflow, пакеты для low-touch, копирование недель между клиентами (релиз 01.2026) | копирование недель; автосообщения — первую ступень |
| PT Distinction (от ≈$20) | глубокая кастомизация, анкеты-ассессменты, брендированные приложения | анкета-онбординг новичка |
| My PT Hub (≈$20) | дешёвый all-in-one на 130 тыс. тренеров | ориентир нижней цены рынка |
| Hevy Coach | вырос из логгера Hevy: лучший ввод подхода — история и рекорды прямо в форме | планка UX дневника |
| TrainHeroic | команды: одна программа на состав, лидерборды, комплаенс по команде, маркетплейс | лидерборды в группах |
| FitBudd ($15–149) | white-label: приложение под именем тренера в сторах | модель «приложение как бренд тренера» — на потом |
| TRNR (от 1000 ₽/мес) | прямой аналог в РФ: конструктор, 500+ видео, группы с лидербордами; тренер — с веба; питания и приёма денег с клиентов нет | в РФ платят от 1000 ₽/мес; приём оплаты с клиентов — свободная ниша |
| FitBase · Fitness365 · FitConnect | CRM клубов, тренер там сотрудник | наш сегмент «частник со своими людьми» ими не закрыт |
| Hevy · Strong · GymUp · GymKeeper | дневники без тренера по ту сторону; у Hevy соцлента и AI-тренер (02.2026) | конкуренты за экран дневника: проиграем ввод — тренер ослепнет |
| OnForm · CoachNow | ниша видеоразбора: замедление, покадрово, рисование, голос, два видео рядом | список инструментов для нашего видеоразбора |
| MyFitnessPal · Cronometer · FatSecret | трекеры питания, которые платформы интегрируют, а не переписывают | свой счётчик калорий не строить |
| Telegram | чат, в котором тренеры живут сейчас | мерило каждого экрана: чем лучше «просто переписки»? |

Что рынок подтверждает у нас (дожимать, не переделывать): асинхронный видеоразбор, привычки,
чек-ины с фото и адгеренсом, группы, программы с циклами, таймер отдыха, офлайн-черновики.
Чего нет ни у кого из них: приватность структурой (клиентская карточка слота не принимает чужое
имя), медпометки, вход через Telegram.

## Фичевая очередь после впуска людей

Пока тренеров ноль, каждая фича — про удержание первых. Порядок: сначала то, что клиент трогает
каждый день, потом то, что экономит час тренеру, потом деньги. Ф5–Ф6 без явного «делаем»
не проектировать.

- **Ф1. Дневник, который не хочется менять на Hevy** — история упражнения и лучший вес в момент
  ввода, личные рекорды с заметным «это рекорд», графики по упражнению (LineChart есть, нет точки
  входа), плейт-калькулятор. _Мерило: клиент с полугодом Hevy не находит причины вернуться._
- **Ф2. Час тренера в неделю** — срез «кто выпадает» на «Сегодня» (данные собираются, среза нет);
  прогрессии в программе («+2,5 кг, если всё сделано», %1RM) и суперсеты; шаблоны программ
  и копирование недель между клиентами; анкета-онбординг с укладкой в медпометки (состав —
  решение); шаблоны сообщений и автоприветствие.
- **Ф3. Видеоразбор до уровня ниши** — замедление и покадрово, линии и углы поверх кадра,
  голосовая заметка, два видео бок о бок (клиент/эталон из справочника).
- **Ф4. Данные с руки** — Health Connect на Android: вес с весов, шаги, сон, пульс — в замеры
  и карточку. HealthKit — после этапа 3.
- **Ф5. Деньги** (решение ваше) — приём оплаты тренером с клиентов (ЮKassa/CloudPayments; TRNR
  не умеет — ниша в РФ свободна); тариф платформы (рынок: $20–60, РФ от 1000 ₽ — продолжение
  квот из 1.2); питание с целей КБЖУ и фото еды, не с конструктора рационов.
- **Ф6. Дальние ставки** (решение ваше) — веб-кабинет тренера (конструктор на телефоне терпим
  до ~10 подопечных), AI (черновик программы, саммари дневника, черновик ответа на чек-ин),
  лидерборды в группах, маркетплейс программ. Фармакология — исключена.

## Техническая очередь

Этапы 1–4 — что мешает впустить людей; здесь — что делает рост безопасным, в порядке риска.

- **Т1. Видеть, что происходит** — продуктовая аналитика (Crashlytics есть, событий нет:
  AppMetrica либо свои события — без неё очередь дальше Ф1 гадание); мониторинг бэкенда
  (healthcheck с внешним пингом и алертом, метрики через Actuator — сейчас о падении расскажет
  пользователь); репетиция восстановления из дампа (не прогонялось ни разу).
- **Т2. Выкатывать без страха** — стейджинг вторым systemd-юнитом со второй базой (дешевле,
  чем чинить Docker); подписанная сборка из тега с автовыкладкой во внутренний трек;
  **force update** — минимальная версия клиента с бэка, поставить до первых внешних
  пользователей, иначе дефект контракта в чужом телефоне живёт вечно.
- **Т3. Данные и надёжность** — пуши без Google-сервисов (Huawei: RuStore Push либо деградация
  в Telegram-бота; отложить до первой жалобы, но помнить); контракт офлайна (что обязано работать
  в подвале спортзала — дневник обязан).
- **Т4. Тесты за пределами авторизации** — паттерн критпутей на моке HTTP растянуть на программы,
  чек-ины, расписание; скриншот-тесты держать при правках дизайна.

## Дизайн: аудит и правки

Хорошо и не трогать: токенная система с парами тем, Golos Text + JetBrains Mono для цифр
(ни у одного конкурента — уже часть лица), приватность структурой, медицинская полоса 4dp,
сдержанная геометрия «инструмента».

Просело, с цифрами:

- контраст `textMuted` 2,5:1 (#A19C93 на #F5F4F2) при норме 4,5:1 — только для декоративного,
  подписи переводить на `textSecondary` (4,9:1);
- чип warning 4,0:1 при кегле 13 Medium — ниже нормы;
- текст кнопки 13sp даже на Large 48dp (у конкурентов 16–17) — Large → 15sp SemiBold,
  Medium → 14sp;
- у Danger нет нажатого состояния — нужен `dangerPressed`;
- `indication = null` в кнопке и карточке — нажатие в списках «мёртвое»;
- `numericBig` 24sp мал для главного числа экрана — нужен `numericHero` 32–36sp;
- тема только системная — нет переключателя в профиле;
- fontScale 130–200 % не прогонялся (открытый вопрос design-report);
- инсеты точечные — единый слот в Scaffolding.

Правки: **Д1** — палитру не менять, добавить «моменты успеха» (анимация рекорда, закрытой недели
привычек, принятого чек-ина; numericHero; вибрация на «подход записан»); **Д2** — кнопки и отклик
одним заходом; **Д3** — скрипт контраста всех пар токенов в обеих темах, валит сборку при
просадке; **Д4** — переключатель темы и прогон крупного шрифта.

## За горизонтом

Оплата, платные разделы и питание разобраны в Ф5, веб-клиент и AI — в Ф6: контекст собран,
решения ваши — без явного «делаем» не проектируется. Раздел фармакологии — исключён,
решение прежнее.

## Как проверять авторизацию, не трогая прод

`androidApp/src/test/.../auth/` — тринадцать критпутей на моке HTTP: регистрация, вход почтой
и логином, занятая почта с указанием поля, неверная пара, блокировка со счётчиком, письмо
и его отказ, ссылка из письма и её смерть, сброс через Telegram, пароль в профиле, выход.
Приём взят у донора: мок отвечает только на то, что тест разрешил явно, а **любой незамоканный
запрос роняет тест** — иначе забытая ручка молча вернула бы пустоту и путь считался бы пройденным.

Слоёв два, и граница между ними важна:

- **`AuthCriticalPathTest` — договор с бэкендом.** Настоящие репозитории поверх `MockEngine`:
  пути ручек, тела запросов, разбор ответов, работа с токенами.
- **`AuthScreenFlowTest` — поведение экранов.** Модели поверх `FakeAuthRepository`, сети нет
  совсем: блокировка отсчитывает пять минут на виртуальном времени, неверная пара не стирает
  набранное, занятая почта подсвечивает своё поле.

Запуск: `gradlew :androidApp:testDebugUnitTest --tests "app.trainer.android.auth.*"` — 26 проверок.
Со стороны бэкенда те же пути закрывают `PasswordAuthServiceTest`, `PasswordResetServiceTest`,
`TelegramLoginServiceTest` и `SessionRotationTest`. Чеклист с привязкой к тестам и список того,
чего тесты не видят, — `swarm-report/password-auth-e2e-scenario.md`.

## Грабли, на которые тут уже наступали

Новому треду прочитать до первой правки — каждая стоила отдельного разбора.

- **`navigator.pop()` после сохранения оставляет родителя со старыми данными.** Лечение —
  `popWithResult(KEY)` + `handleResult(KEY)`. Ловилось трижды: календарь, профиль, прогресс.
- **Экран заведён в DI, но `push` на него нет нигде.** Проверять грепом `Screens.X` при приёмке.
- **Ktor кэширует токен в `BearerAuthProvider`** и перечитывает только после 401: смена
  пользователя без `clearToken()` оставляет запросы от имени предыдущего.
- **Koin: `parametersOf(String)` и `get<String>()` в одном определении несовместимы** — подставится
  параметр, не граф.
- **Новая таблица в `.sq` роняет старые установки** — нужен `.sqm`.
- **Строки Compose живут только в `core_strings` и `uikit`**, иначе не попадут в APK.
- **Миграции проверять `scripts/check_migrations.py`** и сверять с живой схемой: имена ограничений
  и индексов в проде уже кусались.
- **Инсеты точечные, общего механизма нет.** Полноэкранный экран без `AppTopBar` про инсет помнит сам.
- **Значение с пробелами в `/etc/trainer/trainer.env` обязано быть в кавычках.** systemd такую
  строку читает, а `source` из shell-скрипта — нет: `backup.sh` молча падал и дампа не делал.
- **Docker Desktop на машине не стартует** — локального стенда нет, проверка идёт на проде
  через SSH и curl.

## Что где лежит

- `changelog.md` — что и почему уже сделано
- `push-and-media-setup.md` — внешние ключи и аккаунты
- Порядок деплоя, бэкапы, проверка миграций — в памяти проекта
  (`trainer-app-deploy`, `trainer-app-migrations-prod`)
